# Experiments

Each experiment lives in its own subdirectory with a config and results folder:

```
experiments/
  my_experiment/
    config.yaml
    results/
    checkpoints/
```

Results and checkpoints are git-ignored.
